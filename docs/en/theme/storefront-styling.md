# Storefront styling

Fixed Tailwind palette (`warm`, `teal`, `ink`). **No dark UI theme** yet.

<DocScreenshot
  alt="Storefront light theme"
  caption="Storefront home: warm background, teal accents, ink text."
  path="/images/storefront/catalog.png"
  status="placeholder"
/>

Future dark mode: CSS variables + `localStorage`, without rebuilding the full Tailwind config at runtime.
