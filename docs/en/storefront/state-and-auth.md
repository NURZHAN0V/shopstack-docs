# State and auth

**Pinia** for cart, user, site settings (`use*` store names).

JWT: handle 401 with logout; server enforces roles.

Cookies for non-sensitive prefs per `goshop_client/AGENTS.md`.

Do not use provide/inject for catalog business data.
