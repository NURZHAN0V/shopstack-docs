# Testing and CI

- **Unit:** Vitest for composables/stores; Go tests for API
- **E2E:** checkout and login on staging
- **CI:** lint → test → build → deploy

**ShopStack:** `goshop/docs/staging-e2e-checklist.md`, `pnpm build` for docs.
