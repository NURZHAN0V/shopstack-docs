# Storefront deployment

Deploy separately from API/admin stack.

`NUXT_PUBLIC_API_URL` → Go API. Build: `npm run build` in `goshop_client/`.

See `goshop/docs/deploy-docker-registry.md` (storefront section).

<DocScreenshot
  alt="Production storefront"
  caption="Public HTTPS domain with store logo from branding settings."
  path="/images/storefront/production.png"
  status="placeholder"
/>
